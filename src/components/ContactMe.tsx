import { Form } from './Form';

export const ContactMe = () => {
  return (
    <section id="contactMe" className="flex w-full h-screen justify-center items-center">
      <Form />
    </section>
  )
}
